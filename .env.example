# ──────────────────────────────────────────────────────────
# Synapse SAP Explorer — Environment Configuration
# Copy this file to .env and fill in your values
# ──────────────────────────────────────────────────────────

# ── Synapse SDK (required) ───────────────────────────────
SYNAPSE_API_KEY=sk_live_your-synapse-api-key
SYNAPSE_NETWORK=mainnet
# Accepted: US, US-1, EU, EU-1
SYNAPSE_REGION=US-1

# ── Fallback RPC (optional, strongly recommended) ─────────
# Used by sap-sync when Synapse RPC fails on heavy calls
# (notably getProgramAccounts during Metaplex upstream incidents).
# Recommended: a Helius mainnet URL with ?api-key=…
SAP_FALLBACK_RPC_URL=

# ── OOBEP Protocol RPC (optional, for schema scanning) ────
# Primary RPC endpoint for tool schema scanning with round-robin fallback
# Uses intelligent routing: OOBEP (with API key) → Mainnet → Free RPCs
OOBEP_RPC_URL=https://us-1-mainnet.oobeprotocol.ai
OOBEP_API_KEY=your-oobep-api-key

# ── Public RPC endpoints (optional, for round-robin) ──────
# Free Solana RPC endpoints used as tertiary fallback
# Leave empty to use defaults: api.mainnet-beta.solana.com, rpc.ankr.com, projectserum.com
SOLANA_FREE_RPC_1=
SOLANA_FREE_RPC_2=
SOLANA_FREE_RPC_3=

# ── Database (required) ──────────────────────────────────
DATABASE_URL=postgresql://user_db_sap_exp:your-password@localhost:5432/DB_SAP_EXP

# ── Indexer mode (optional) ───────────────────────────────
# polling | stream | hybrid
INDEXER_MODE=hybrid
# DB-first default: disable periodic full scans (getProgramAccounts).
# Set true only for manual/bootstrap/healing windows.
ENTITY_FULL_SCAN_ENABLED=false
# Optional: dedicated RPC URL only for indexer process (polling/account refresh/full scans).
# Leave empty to use Synapse resolver from SYNAPSE_NETWORK/SYNAPSE_REGION.
INDEXER_RPC_URL=
# Optional RPC debug logs (masked URL/API key): true | false
INDEXER_RPC_DEBUG=false
# processed | confirmed | finalized (for grpc stream mode)
INDEXER_GRPC_COMMITMENT=confirmed
# Throttle for entity_delta cursor updates during account stream ingest.
ENTITY_DELTA_CURSOR_TOUCH_INTERVAL_MS=5000

# Indexer healthcheck behavior
HEALTH_DB_FIRST=true
HEALTH_STRICT_ENTITY_FRESHNESS=false
HEALTH_ENTITY_DELTA_MAX_AGE_MS=22500000
HEALTH_ENTITY_TABLE_MAX_AGE_MS=604800000

# ── Public API v1 (optional) ──────────────────────────────
# Comma-separated fallback keys used only if DB keys table is unavailable.
PUBLIC_API_FREE_KEYS=
PUBLIC_API_PRO_KEYS=
# CORS allowlist for /api/v1/* (comma-separated origins). Use * for open.
PUBLIC_API_CORS_ORIGINS=*

SAP_SYNC_AUTOSTART=